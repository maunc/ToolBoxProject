<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <RelativeLayout
        android:id="@+id/common_tool_bar_layout"
        android:layout_width="match_parent"
        android:layout_height="@dimen/common_tool_bar_height">

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/common_tool_bar_back_button"
            android:layout_width="@dimen/common_tool_bar_icon_size"
            android:layout_height="@dimen/common_tool_bar_icon_size"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:layout_marginStart="@dimen/dp_24"
            android:scaleType="centerCrop"
            android:src="@drawable/icon_back" />

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/common_tool_bar_title_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:textColor="@color/black"
            android:textSize="23sp"
            android:textStyle="bold"
            tools:ignore="RelativeOverlap" />

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/common_tool_bar_compat_button"
            android:layout_width="@dimen/common_tool_bar_icon_size"
            android:layout_height="@dimen/common_tool_bar_icon_size"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:layout_marginEnd="@dimen/dp_24"
            android:scaleType="centerCrop"
            android:tint="@color/black" />
    </RelativeLayout>
</layout>